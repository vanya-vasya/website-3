function unBindWindow() {
    $(window).unbind("beforeunload", ShowAlert)
}

function ShowAlert() {
    return exitAlert
}

$(window).bind("beforeunload", ShowAlert), "" != unBindElements && $(unBindElements).bind("click", unBindWindow);