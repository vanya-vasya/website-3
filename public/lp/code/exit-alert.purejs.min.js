/*$(window).on("beforeunload", ShowAlert);
if (unBindElements != "") {
	$(unBindElements).on("click", unBindWindow);
}

function unBindWindow() {
	$(window).off("beforeunload", ShowAlert);
}

function ShowAlert() {
	return exitAlertMessage;
}
*/

window.addEventListener('onbeforeunload', function() {
	return exitAlert;
}